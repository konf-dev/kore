# Core dependencies
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
bitsandbytes>=0.43.0

# Fast inference (optional but recommended)
vllm>=0.4.0

# Utilities
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.66.0
jsonlines>=4.0.0
rich>=13.7.0

# Metrics and logging
wandb>=0.16.0
tensorboard>=2.16.0

# Testing
pytest>=8.0.0
